import React from 'react';
import styled from "styled-components";

const Index = ({title}) => {
    return (
        <div>
            <h1>{title}</h1>
        </div>
    );
}

export default Index;
